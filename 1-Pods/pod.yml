
apiVersion: v1 #version of the API to use
kind: Pod #What kind of object we're deploying
metadata: #information about our object we're deploying
  name: flaskff-pod
spec: #specifications for our object
  containers:
  - name: flaskaa-container #the name of the container within the pod
    image: yenaing/python_flask #which container image should be pulled
    ports:
    - containerPort: 88 #the port of the container within the pod
